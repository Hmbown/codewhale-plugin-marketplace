# Extension security source pass — September 11, 2026

The source audit of marketplace `d6464d0` recorded eight findings: six medium
and two low. All eight now have source fixes. This receipt records local
remediation and regression evidence, not production security clearance.
The original workbench report remains an immutable description of the audited
snapshot; its findings are not a new scan of the patched source.

| Area | Source change | Regression evidence |
| --- | --- | --- |
| WhaleWiki generated files | Validate wiki/output directories and leaves, including dangling links; use exclusive temporary files and atomic replacement; validate INDEX before reading it for marking | Real CLI fixtures cover scaffold, map, mark, receipt and export, linked parents, macOS path aliases and hardlink preservation |
| Windows application launch | Pass the URL as encoded data and quote it as one application argument | Inert runner checks substitutions, Unicode, quotes, trailing backslashes and invalid inputs |
| Harmony application launch | Validate bundle/ability identifiers and quote device-shell operands | Malformed operands cause zero dispatches; valid identifiers preserve the launch contract |
| Linux screenshot output | Require absolute output filenames before probing/capture; normalize the configured output directory | An option-shaped path cannot reach the capture runner; an ordinary absolute filename can |
| Telegram and Feishu recovery | Persist admitted sender identity; recheck current allowlist and group policy before recovery reads or delivery; change Feishu reply context only after admission | Saved-state fixtures cover revoked access, disabled groups, missing/mismatched identities, allowed recovery and fresh message/callback admission |
| Review skill | Prefer installed read-only wiki tools; read repository prose as unverified text when unavailable | Source review confirms automatic repository-verifier execution was removed from both skill copies |
| macOS pointer ownership | Visible foreign windows occlude the pointer at every window layer, including faint overlays | Compiled Objective-C selection code runs against synthetic window lists; no global pointer input is sent |
| Imported parity reports | Validate platform/session/date and task metadata before output; constrain generated filenames | Traversal fixtures leave external sentinels and report directories unchanged |

## Verification

The required marketplace command `npm test && npm run check:web` passed on
macOS: **384 Node tests, 369 passed, 15 platform skips, zero failures**, plus
**four Playwright checks passed** (desktop and mobile Chromium).

| Node suite | Passed | Skipped |
| --- | ---: | ---: |
| WhaleWiki | 36 | 0 |
| Computer Use | 217 | 15 |
| Shared bridge helpers and recovery | 16 | 0 |
| Feishu | 19 | 0 |
| Telegram | 39 | 0 |
| Slack/Linear webhook adapter | 13 | 0 |
| WeCom | 16 | 0 |
| WeChat/Weixin | 3 | 0 |
| Marketplace contracts and packaging | 10 | 0 |

Fifteen targeted negative controls failed with the previous behavior: five wiki
checks, four Computer Use argument/report checks, five bridge checks, and the
native window-selection check with its former layer policy restored in an
isolated fixture. The review-skill change is prose, reviewed directly.

Catalog validation, source-mirror checks and the three-page wiki freshness gate
also passed. The source audit covered 231 of 232 present repository files.
The committed macOS launcher binary was not reverse-engineered or proven equal
to its reviewed source. Native dependencies, live provider APIs, service-account
authorization and OS-specific runtime behavior require separate acceptance.
A cross-user local socket/pipe permission question remains unproven.

## Source ownership and operational limits

Computer Use fixes originate at canonical commit `1d1143d`; the marketplace
mirror and Core's carried runtime/test subset match it. The previously divergent
Core route-test fixture now also matches upstream. Core commit `64bf603c7a`
owns the bridge changes and bundled review skill; `integrations/upstream.json`
pins the committed bridge files and their exact hashes.

Legacy Telegram/Feishu saved state without verified sender provenance stays
detached on restart. This does not cancel an existing runtime turn. Use an
admitted chat to inspect or interrupt it; new admitted messages record identity
for later recovery. These adapters share an operator runtime and do not provide
hosted tenant isolation.

No live service login, bot message, model turn, desktop input/capture, install,
public push or deployment was performed for this security pass. Windows, Linux
and Harmony launch/capture tests used inert runner seams. Native macOS tests
used synthetic fixtures. The earlier installed-host receipt in
[VALIDATION-20260911.md](VALIDATION-20260911.md) describes its original package
hash, not this patched package. Hosted CI and installed acceptance of these
patches remain separate work.
